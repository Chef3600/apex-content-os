Generated proposals land here, one file per prospect, written by:

    node tools/pipeline.mjs proposal <id> <amount>

They are documents, not sends. Read the whole thing before it goes anywhere -
the tool fills the price and the facts from the record, it does not judge
whether the scope is right.

Terms behind every one: `docs/scope-and-terms.md`.
